name: CI - Build y Test
on: [push, pull_request]
jobs:
  build:        # <--- 2 espacios de sangría
    runs-on: ubuntu-latest  # <--- 4 espacios
    steps:      # <--- 4 espacios
      - uses: actions/checkout@v4  # <--- 6 espacios
      - name: Instalar dependencias
        run: echo "Instalando..."
      - name: Ejecutar tests
        run: echo "Tests pasados con éxito"
